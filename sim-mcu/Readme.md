# Descripcion
Logica del simulador portada al mcu(ESP32-S3-WROOM1). En este caso utilizamos un toolchain de rust pensado para mcus Espressif ESP-x. 
Para generar proyectos para este mcu, usamos esp-generate (https://github.com/esp-rs/esp-generate)
